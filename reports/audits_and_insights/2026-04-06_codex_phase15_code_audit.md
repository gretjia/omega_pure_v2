PASS grad_accum: `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:115-116,126-156` requires divisibility, `1/ACCUM` loss scaling, and scheduler stepping on optimizer steps; `train.py:391-396` scales loss by `1/grad_accum`, `train.py:415-427` gates `optimizer.step()` and `scheduler.step()` behind `(step_i + 1) % grad_accum == 0`, `train.py:605-608` rejects non-divisible `steps_per_epoch`, and `train.py:767-774` computes OneCycle `total_steps` from optimizer steps.
PASS EMA: `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:161-190` requires `get_ema_multi_avg_fn`, no `update_bn()`, and LR switching at EMA start; `train.py:809-810` constructs `AveragedModel(model, multi_avg_fn=get_ema_multi_avg_fn(args.ema_decay))`, `train.py:825-833` switches param-group LR and disables `OneCycleLR` at `ema_start_epoch`, `train.py:850-853` updates EMA after each eligible epoch, and `train.py:920-946` saves/evaluates EMA directly with no observable `update_bn()` call in `train.py`.
PASS embargo gap: `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:84-92,120` requires dropping both sides of the train/val gap; `train.py:658-659` slices `train_shards = valid_shards[:n_train - emb]` and `val_shards = valid_shards[n_train + emb:]`, and `train.py:661-663` logs `emb*2` total dropped.
PASS MLP baseline: `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:272-305` requires preserving SRL+FRT while remaining interface-compatible with the main model; `train.py:116-139` defines `MLPBaseline` returning `(pred, dummy_z)`, `train.py:204-205,274` shows the main model signature/return shape, and `train.py:689-734` keeps SRL via `srl_inverter`, recreates the FRT transform, and feeds the resulting 6-D manifold into an adapter with `forward(self, x_2d, c_friction)`.
FAIL RPB (relative positional bias): `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:121,195-206` and `architect/current_spec.yaml:211-212` require per-epoch RPB gradient monitoring, but `train.py:861-865` only populates `rpb_grad_str` when a `relative_position_bias_table` parameter exists and `param.grad is not None`, so the epoch log at `train.py:868-877` does not guarantee an RPB metric every epoch.
WARNING YAML config: the Step 1 Plan defaults at `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:115-122,214-245` match `gcp/phase15_step1_config.yaml:11-31,33-34` on every explicitly listed value, but `gcp/phase15_step1_config.yaml:32` adds `--model_type=omega`; the Plan omits that arg in the Step 1 YAML block while implying it in `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:301-305`, so the prompt’s “all 21 parameters match exactly” check is ambiguous from the Plan text.
PASS spec phase15 section in current_spec.yaml aligns with Plan V2: the Phase 15 spec block at `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:464-485` plus the MLP baseline requirements at `architect/directives/2026-04-06_phase15_pipeline_stabilization.md:272-305` are reflected in `architect/current_spec.yaml:198-216` (gradient accumulation, optimizer-step counts, EMA details, embargo, evaluation, RPB monitoring, and `mlp_baseline.preserves_srl_frt: true`).
PASS gcp/train.py vs train.py: full-file byte comparison passed (`cmp_exit=0` with identical SHA-256 hashes) for `train.py:1-967` and `gcp/train.py:1-967`; both files also have the same line count (`train.py:967`, `gcp/train.py:967`).
