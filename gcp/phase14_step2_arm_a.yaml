workerPoolSpecs:
- machineSpec:
    machineType: n1-standard-8
    acceleratorType: NVIDIA_TESLA_T4
    acceleratorCount: 1
  replicaCount: 1
  diskSpec:
    bootDiskType: pd-standard
    bootDiskSizeGb: 100
  containerSpec:
    imageUri: us-central1-docker.pkg.dev/gen-lang-client-0250995579/omega-training/omega-tib:phase14-vstep2
    command:
    - bash
    - -c
    - |
      set -e
      echo "=== Phase 14 Step 2 — Arm A: Baseline (no macro bypass) ==="
      echo "Control arm: Phase 13 architecture, 5 epochs, pipe I/O"
      echo "Purpose: Reproduce Phase 13 baseline for A/B comparison"

      PYTHONUNBUFFERED=1 python3 /app/train.py \
        --shard_dir=gs://omega-pure-data/wds_shards_v3_full \
        --output_dir=/gcs/omega-pure-data/checkpoints/phase14_step2_arm_a \
        --hidden_dim=64 \
        --window_size_t=32 \
        --window_size_s=10 \
        --lr=3e-4 \
        --lambda_s=0 \
        --batch_size=256 \
        --coarse_graining_factor=1 \
        --epochs=5 \
        --steps_per_epoch=5000 \
        --num_workers=6 \
        --ckpt_every_n_steps=500 \
        --no_amp \
        --grad_clip=1.0 \
        --mask_prob=0.0 \
        --macro_bypass=False \
        --seed=42

      echo "=== Arm A Complete ==="
      echo "Checkpoints: gs://omega-pure-data/checkpoints/phase14_step2_arm_a/"
scheduling:
  strategy: ON_DEMAND
